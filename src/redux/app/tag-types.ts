export enum tagTypes {
    user = "user",
    category = "category",
    service = "service",
    faq = "faq",
    blog = "blog",
}

export const tagTypesList = [
    tagTypes.user,
    tagTypes.category,
    tagTypes.service,
    tagTypes.faq,
    tagTypes.blog,
];
