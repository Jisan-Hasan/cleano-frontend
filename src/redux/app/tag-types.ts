export enum tagTypes {
    user = "user",
    category = "category",
    service = "service",
    faq = "faq",
}

export const tagTypesList = [
    tagTypes.user,
    tagTypes.category,
    tagTypes.service,
    tagTypes.faq
];
